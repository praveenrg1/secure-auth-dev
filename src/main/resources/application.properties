spring.application.name=secureauth

server.port=8069
# Enable the H2 console
spring.h2.console.enabled=true

# (Optional) Customize the console path (default is /h2-console)
spring.h2.console.path=/h2-console

# (Optional) Configure the datasource URL, username, and password
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=praveen
spring.datasource.password=Password1!
spring.datasource.driverClassName=org.h2.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

##jwt secret grt when app start from enviroinment ##
jwt.secret=${JWT_SECRET}